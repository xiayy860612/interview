#server.port=9090

# evn setting
spring.profiles.active=dev

# mybatis config
mybatis.config-location=classpath:config/mybatis-config.xml
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=com.s2u2m.example.statistic.entity
mybatis.mappers-package=com.s2u2m.example.statistic.dao
